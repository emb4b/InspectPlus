import { View, Text } from 'react-native';

export default function SurveyReportScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Survey Report — Week 4</Text>
    </View>
  );
}